class ResultKeys:
    RESULT_DATA = "result_data"
    CONFIG = "config"


class ConfigKeys:
    START = "start"
    END = "end"
    NUM_STEPS = "num_steps"
    STEP_SIZE = "h"
    METRICS = "metrics"
    CALLBACKS = "callbacks"
    TIMESTAMP = "timestamp"
    ID = "result_id"
    LOOP_TYPE = "loop_type"


class ModelMetadataKeys:
    DIM_NAMES = "dim_names"
    VARIABLE_NAMES = "variable_names"
